#ifndef UI_UI_H
#define UI_UI_H

#include "events.h"
#include "instructions.h"

#include "Component.h"
#include "Platform.h"
#include "Window.h"
#include "Panel.h"
#include "Button.h"
#include "Line.h"

#include <stent.h>

/*
  Typedefs not needed due to stent's ref(T).

  typedef struct InitEvent InitEvent;
  typedef struct ClickEvent ClickEvent;
  typedef struct EnterEvent EnterEvent;
*/

#endif
